## 2026-02-20 - Missing ARIA Labels on Icon Buttons
**Learning:** Custom icon buttons often lack `aria-label` and visible focus states, making them inaccessible to screen readers and keyboard users.
**Action:** Always inspect custom buttons for accessibility attributes and ensure they have visible focus indicators (`focus:ring`, `outline-none`).
